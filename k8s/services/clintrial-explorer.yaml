apiVersion: v1
kind: Service
metadata:
  name: clintrial-explorer
  namespace: wip
  labels:
    app.kubernetes.io/name: clintrial-explorer
    app.kubernetes.io/part-of: wip
spec:
  type: ClusterIP
  ports:
    - port: 3013
      targetPort: 3013
  selector:
    app.kubernetes.io/name: clintrial-explorer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clintrial-explorer
  namespace: wip
  labels:
    app.kubernetes.io/name: clintrial-explorer
    app.kubernetes.io/part-of: wip
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: clintrial-explorer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: clintrial-explorer
        app.kubernetes.io/part-of: wip
    spec:
      containers:
        - name: clintrial-explorer
          image: localhost:32000/clintrial-explorer:latest
          ports:
            - containerPort: 3013
          env:
            - name: WIP_BASE_URL
              value: "https://wip-kubi.local"
            - name: WIP_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wip-secrets
                  key: api-key
            - name: WIP_NAMESPACE
              value: "clintrial"
            - name: NODE_TLS_REJECT_UNAUTHORIZED
              value: "0"
            - name: PORT
              value: "3013"
          readinessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clintrial-explorer-ingress
  namespace: wip
  labels:
    app.kubernetes.io/name: clintrial-explorer
    app.kubernetes.io/part-of: wip
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - wip-kubi.local
      secretName: wip-tls
  rules:
    - host: wip-kubi.local
      http:
        paths:
          - path: /apps/clintrial(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: clintrial-explorer
                port:
                  number: 3013
