# WIP Registry Service Dependencies (Docker build)
# Note: wip-auth is mounted and installed at runtime via docker-compose

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# MongoDB
motor>=3.3.0,<4.0.0
beanie>=1.25.0,<2.0.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# File upload support
python-multipart>=0.0.6

# HTTP client (for proxy queries)
httpx>=0.26.0

# Environment variables
python-dotenv>=1.0.0

# Security
python-jose[cryptography]>=3.3.0
