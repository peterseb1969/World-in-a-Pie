# WIP Reporting Sync Service Dependencies (Docker build)
# Note: wip-auth is mounted and installed at runtime via docker-compose

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
asyncpg>=0.29.0
sqlalchemy>=2.0.0

# Message queue
nats-py>=2.6.0

# HTTP client (for Template Store API)
httpx>=0.26.0

# Utilities
python-json-logger>=2.0.0
