# WIP App: React Console
# Drop this file into your WIP install directory and re-run setup-wip.sh.
# See docs/install-test-guide.md for details.

services:
  react-console:
    image: gitea.local:3000/peter/react-console:test2
    container_name: wip-react-console
    labels:
      wip.app: "true"
      wip.app.name: "React Console"
      wip.app.route: "/apps/rc"
      wip.app.port: "3011"
      wip.app.oidc: "true"
      wip.app.oidc.client_id: "react-console"
      wip.app.oidc.client_secret: "react-console-secret"
    env_file: .env
    environment:
      WIP_BASE_URL: http://wip-caddy:8080
      WIP_API_KEY: ${API_KEY}
      OIDC_ISSUER: https://${WIP_HOSTNAME:-localhost}:8443/dex
      OIDC_CLIENT_ID: react-console
      OIDC_CLIENT_SECRET: react-console-secret
      PORT: "3011"
      APP_BASE_PATH: /apps/rc
      NODE_ENV: production
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
    networks:
      - wip-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3011/apps/rc/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  wip-network:
    external: true
