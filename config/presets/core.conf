# WIP Preset: Core
#
# Minimal deployment with API-key authentication only.
# No OIDC, no SQL reporting, no file storage.
#
# Use case:
#   - Single-user setups
#   - Embedded/headless deployments
#   - Lowest resource footprint
#
# Services: MongoDB, NATS, Registry, Def-Store, Template-Store, Document-Store, Console
# Auth: API keys only (X-API-Key header)
#

PRESET_NAME="core"
PRESET_DESCRIPTION="Minimal - API keys only, no OIDC"

# Modules to include
MODULES="console"
