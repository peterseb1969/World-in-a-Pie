# WIP Preset: Standard
#
# Recommended for most deployments.
# OIDC authentication, SQL reporting via PostgreSQL, and file storage via MinIO.
#
# Use case:
#   - Multi-user setups with login
#   - Network-accessible deployments
#   - SQL analytics, cross-template joins, BI tool integration
#   - File uploads and binary storage
#
# Services: MongoDB, NATS, PostgreSQL, MinIO, Dex, Caddy, Registry, Def-Store,
#           Template-Store, Document-Store, Reporting-Sync, Console
# Auth: OIDC (Dex) + API keys
#

PRESET_NAME="standard"
PRESET_DESCRIPTION="Standard - OIDC, SQL reporting, file storage"

# Modules to include
MODULES="console,oidc,reporting,files"
