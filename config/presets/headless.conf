# WIP Preset: Headless
#
# API-only deployment without WIP Console.
# Lowest footprint - ideal for MCP-only workflows or embedded use.
#
# Use case:
#   - AI/MCP-only access (no browser UI needed)
#   - Embedded systems with tight resource constraints
#   - Backend-only deployments accessed via API key
#
# Services: MongoDB, Registry, Def-Store, Template-Store, Document-Store
# Auth: API keys only (X-API-Key header)
#

PRESET_NAME="headless"
PRESET_DESCRIPTION="Headless - API only, no Console, no OIDC"

# Modules to include (no console, no oidc)
MODULES=""
